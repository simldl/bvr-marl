"""
Services layer — shared business logic used by both GUI and CLI.

Modules:
    training     — command building, config discovery, background process launch
    visualization — visualization command construction, checkpoint discovery
    tacview       — Tacview scenario command construction
    configs       — unified config discovery and loading wrappers
    processes     — generic background process monitoring
"""
