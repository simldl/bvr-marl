"""Public / baseline entry points for bvr_marl_core.

These scripts mirror the simple pipeline shipped with the core bvr-marl package
and are intentionally kept free of custom neural-network architectures so that
users can run training and visualization without an extended model stack.

A full custom-network pipeline can be provided by an extension package that
builds on and extends these baselines.
"""
