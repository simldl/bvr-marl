# ── Python ───────────────────────────────────────────────────────────────────
*.py[cod]
*.so
*.pyd
__pycache__/
*.egg-info/
*.egg
.eggs/
dist/
build/
.installed.cfg
lib/
lib64/
sdist/
wheels/
*.whl

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/
.conda/

# ── IDE / Editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Personal / AI assistant tooling (not part of the project) ─────────────────
.claude/
.serena/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Testing / Quality tools ───────────────────────────────────────────────────
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Node.js ───────────────────────────────────────────────────────────────────
node_modules/
**/node_modules/

# ── Runtime state (generated during execution, never committed) ───────────────
runtime/
state/

# Process state files (generated by GUI process tracker)
gui/*.json
src/gui/*.json
gui/logs/
src/gui/logs/

# ── Root-level visualization workspace (symbol generation tooling, not part of the package) ──
/visualization/

# ── Training outputs ──────────────────────────────────────────────────────────
models/
outputs/
logs/
reward_logs/
*.log


# ── Tacview output files ──────────────────────────────────────────────────────
tacview/logs/
*.acmi

# ── Visualization generated outputs ──────────────────────────────────────────
profiling_results/output/
profiling_results/*.stats
profiling_results/*.txt
src/visualization/tensorboard_exports/
# Generated PNGs (but keep committed symbol PNGs)
*.png
!src/air_to_air_rl/visualization/symbols/png/**/*.png

# ── Temp files written at root during visualization development ───────────────
temp_*.png
temp_*.jpg
ground_tracks.png

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
*.env
*.env.*

# ── Streamlit ────────────────────────────────────────────────────────────────
# NOTE: .streamlit/config.toml IS intentionally tracked (shared project config).
# Do NOT add .streamlit/ here.

# ── Misc ─────────────────────────────────────────────────────────────────────
*.sqlite3
*.gif
.cache/
